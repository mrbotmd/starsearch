import React from "react";

const ResultItem = (props) => {
  return (
    <div>
      <p>result</p>
      {/* {items.map((item) => (
        <div>{item.id}</div>
      ))} */}
    </div>
  );
};

export default ResultItem;
